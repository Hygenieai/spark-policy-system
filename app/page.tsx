/*
  Design philosophy: Editorial Institutional Modernism.
  The server page stays intentionally thin so the full experience can later migrate into
  the Spark website as app/policy-system/page.tsx with minimal changes.
*/

import LandingPage from "@/components/landing/landing-page";

export default function Page() {
  return <LandingPage />;
}
